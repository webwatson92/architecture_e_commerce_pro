enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delevered }

enum PaymentMethos { paypal, googlePay, applePay, visa, masterCard, creditCard, paystack, razorPay, paytm}